query($pageNumber: Int!, $pageSize: Int!) {
    documents(
      paging: {
        pageSize: $pageSize,
        pageNumber: $pageNumber,
        sort:{ 
          orders: [{direction: ASC, property: "modTime"}]
        }
      }  
    ) {
    content {
        id
    }
    page {
        totalElements
        totalPages
        number
        size
      }
  }
}