query {
    documentsPaged(paging: {
        pageNumber: 0 pageSize: 10000,
        sort:{ 
          orders: [{direction: ASC, property:"modTime"}]
        }
      }
    ) {
    content {
        id
    }
  }
}