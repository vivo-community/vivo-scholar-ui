query($id: String!) {
  person(id: $id) {
    id
    name {
      firstName
      middleName
      lastName
    }
    primaryTitle
    affliationList {
      id
      label
      startDate {
        dateTime
        resolution
      }
    }
    image {
      thumbnail
    }
  }
}